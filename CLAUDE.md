# 3d-assets

A CC0 game-asset library. See [AGENTS.md](AGENTS.md) for how to find and use the
assets: repository layout, the generated manifests in `index/`, sprite-sheet
atlas formats, and the conventions for adding packs.

There is no build or test suite here - it is data only.
